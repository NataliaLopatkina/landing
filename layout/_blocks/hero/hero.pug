mixin hero(obj)

    div(class="hero hero--" + obj.mod)
        div(class="box hero__box")
            h1(class="title title--white hero__title") !{obj.title}
            h2(class="text text--large text--white hero__subtitle") !{obj.subtitle}
            !="\n"
            include ../../_blocks/button/button.pug
            +button({
                tag: "a",
                href: "#5",
                text: "Узнать больше",
                mod: ["fill", "small"]
            })(class="hero__button")

    !="\n"
    // END .hero
